name: mundial

build:
  frontend:
    context: frontend
deploy:
  - helm upgrade --install mundial chart --set frontend.image=${OKTETO_BUILD_FRONTEND_IMAGE}
dev:
  frontend:
    command: yarn start
    sync:
      - frontend:/usr/src/app

